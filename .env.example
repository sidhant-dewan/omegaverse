# Optional in V1. The local development catalog works without credentials.
TMDB_API_KEY=

# Enable only when TVmaze schedule enrichment is wanted.
TVMAZE_ENABLED=false

# Production URL used for canonical and Open Graph metadata.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
