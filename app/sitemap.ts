import type { MetadataRoute } from "next";
import { people, titles } from "@/lib/data/catalog";
export default function sitemap(): MetadataRoute.Sitemap { const base = process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000"; const staticRoutes = ["","/bl","/gl","/omegaverse","/explore","/airing","/upcoming","/charts","/search","/suggest"]; return [...staticRoutes.map((path) => ({ url: `${base}${path}`, lastModified: new Date(), changeFrequency: "daily" as const, priority: path === "" ? 1 : .8 })), ...titles.map((title) => ({ url: `${base}/title/${title.slug}`, lastModified: new Date(title.addedAt), changeFrequency: "weekly" as const, priority: .7 })), ...people.map((person) => ({ url: `${base}/person/${person.slug}`, changeFrequency: "monthly" as const, priority: .5 }))]; }
